# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/weighbridge

# Serial Port Configuration
SERIAL_PORT=/dev/ttyUSB0
# On Windows use: COM3
BAUD_RATE=9600

# IP Camera Streams
CAMERA1_URL=rtsp://admin:password@192.168.1.100:554/stream1
CAMERA2_URL=rtsp://admin:password@192.168.1.101:554/stream1
# Or HTTP snapshot URLs:
CAMERA1_SNAPSHOT=http://192.168.1.100/snapshot.jpg
CAMERA2_SNAPSHOT=http://192.168.1.101/snapshot.jpg

# ── PRINTER SETTINGS ─────────────────────────────────────────
# PRINTER_TYPE options:
#   local  = use OS print queue (Windows/Linux/macOS)
#   ip     = send directly to network printer via RAW port 9100
#   html   = return HTML for browser window.print() (no server printing)
#   pdf    = generate PDF via wkhtmltopdf and send to local printer
PRINTER_TYPE=local

# Local printer name (leave blank for system default)
# Windows: "HP LaserJet Pro M404dn"
# Linux:   "HP_LaserJet" (from lpstat -a)
PRINTER_NAME=

# IP / Network Printer
PRINTER_IP=192.168.1.200
PRINTER_PORT=9100          # RAW/JetDirect port (standard: 9100)

# Paper size in mm (210 = A4, 80 = 80mm receipt, 76 = 3-inch receipt)
PRINTER_WIDTH_MM=210

# PDF engine: wkhtmltopdf | puppeteer (must be installed separately)
PDF_ENGINE=wkhtmltopdf

# ── COMPANY / RECEIPT DETAILS ──────────────────────────────
COMPANY_NAME=SRI VENKADESWARA WEIGH BRIDGE
COMPANY_ADDR1=CHENNAI-THIRUVANAMALAI BYEPASS ROAD
COMPANY_ADDR2=NEAR BY SANDHAI MEDU . THINDIVANAM - 604 001
COMPANY_PHONE=Ph : 9994706523 . 9543389898
COMPANY_LOGO=    # Optional: path to logo image or base64 data URI

# Server Port
PORT=3001
